export const PAGINATION_CONFIG = {
    /** Key to get current page in request query */
    pageKey: 'page',
    /** Key to get number of items in request query */
    perPageKey: 'perPage',
    /** Default number of items per page */
    defaultPerPage: 10,
    /** Max number of items per page */
    maxPerPage: 500,
};
